const express = require("express");
const { Client, RichEmbed } = require("discord.js");
const app = express();
const chalk = require("chalk");
const fs = require("fs");
const ytdl = require("ytdl-core");
function sleep(milliseconds) {
  var start = new Date().getTime();
  for (var i = 0; i < 1e7; i++) {
    if (new Date().getTime() - start > milliseconds) {
      break;
    }
  }
}
const http = require("http");
app.get("", (request, response) => {
  console.log(Date.now() + " Ping tamamdır.");
  response.sendStatus(200);
});


const Discord = require("discord.js");
const client = new Discord.Client();
const data = new Map();

const ta1 = new Discord.Client();794209348338581556
const ta2 = new Discord.Client();794209348338581556
const ta3 = new Discord.Client();794209348338581556
const ta4 = new Discord.Client();794209348338581556
const ta5 = new Discord.Client();794209348338581556
const ta6 = new Discord.Client();794209348338581556
const ta7 = new Discord.Client();794209348338581556
const ta8 = new Discord.Client();794209348338581556
const ta9 = new Discord.Client();794209348338581556
const ta10 = new Discord.Client();794209348338581556
const ta11 = new Discord.Client();794209348338581556
const ta12 = new Discord.Client();794209348338581556
const ta13 = new Discord.Client();794209348338581556
const ta14 = new Discord.Client();794209348338581556
const ta15 = new Discord.Client();794209348338581556
const ta16 = new Discord.Client();794209348338581556
const ta17 = new Discord.Client();794209348338581556
const ta18 = new Discord.Client();794209348338581556
const ta19 = new Discord.Client();794209348338581556
const ta20 = new Discord.Client();794209348338581556
const ta21 = new Discord.Client();794209348338581556
const ta22 = new Discord.Client();794209348338581556
const ta23 = new Discord.Client();794209348338581556
const ta24 = new Discord.Client();794209348338581556
const ta25 = new Discord.Client();794209348338581556
const ta26 = new Discord.Client();794209348338581556
const ta27 = new Discord.Client();794209348338581556
const ta28 = new Discord.Client();794209348338581556
const ta29 = new Discord.Client();794209348338581556
const ta30 = new Discord.Client();794209348338581556
const ta31 = new Discord.Client();794209348338581556
const ta32 = new Discord.Client();794209348338581556
const ta33 = new Discord.Client();794209348338581556
const ta34 = new Discord.Client();794209348338581556
const ta35 = new Discord.Client();794209348338581556
const ta36 = new Discord.Client();794209348338581556
const ta37 = new Discord.Client();794209348338581556
const ta38 = new Discord.Client();794209348338581556
const ta39 = new Discord.Client();794209348338581556
const ta40 = new Discord.Client();794209348338581556
const ta41 = new Discord.Client();794209348338581556
const ta42 = new Discord.Client();794209348338581556
const ta43 = new Discord.Client();794209348338581556
const ta44 = new Discord.Client();794209348338581556
const ta45 = new Discord.Client();794209348338581556
const ta46 = new Discord.Client();794209348338581556
const ta47 = new Discord.Client();794209348338581556
const ta48 = new Discord.Client();794209348338581556
const ta49 = new Discord.Client();
const ta50 = new Discord.Client();
const ta51 = new Discord.Client();
const ta52 = new Discord.Client();
const ta53 = new Discord.Client();
const ta54 = new Discord.Client();
const ta55 = new Discord.Client();
const ta56 = new Discord.Client();
const ta57 = new Discord.Client();
const ta58 = new Discord.Client();
const ta59 = new Discord.Client();
const ta60 = new Discord.Client();
const ta61 = new Discord.Client();
const ta62 = new Discord.Client();
const ta63 = new Discord.Client();
const ta64 = new Discord.Client();
const ta65 = new Discord.Client();
const ta66 = new Discord.Client();
const ta67 = new Discord.Client();
const ta68 = new Discord.Client();
const ta69 = new Discord.Client();
const ta70 = new Discord.Client();
const ta71 = new Discord.Client();
const ta72 = new Discord.Client();
const ta73 = new Discord.Client();
const ta74 = new Discord.Client();
const ta75 = new Discord.Client();
const ta76 = new Discord.Client();
const ta77 = new Discord.Client();
const ta78 = new Discord.Client();
const ta79 = new Discord.Client();
const ta80 = new Discord.Client();
const ta81 = new Discord.Client();
const ta82 = new Discord.Client();
const ta83 = new Discord.Client();
const ta84 = new Discord.Client();
const ta85 = new Discord.Client();
const ta86 = new Discord.Client();
const ta87 = new Discord.Client();
const ta88 = new Discord.Client();
const ta89 = new Discord.Client();
const ta90 = new Discord.Client();
const ta91 = new Discord.Client();


 ta1.login("Nzk1MjY2MzQ2NjczMzczMjE0.X_G3kw.g3Fijvgcd0oNd0YV8tzkoP9o_B8");
 ta2.login("Nzk1MjY2ODM2MzgzNjYyMTMx.X_G4Kg.uUE8hm9K1R-h0QfweM6sTW5zFWU");
 ta3.login("Nzk1MjY3NjA3MDkxMzQ3NDc3.X_G4vA.N2N-mOBTCAhkQhXxVLwyImZeGSI");
 ta4.login("Nzk1MjY4MDEwOTY4NzQzOTk2.X_G5Uw.1Y3HOiEBOhXgU-l0uLvga-DEgAI");
 ta5.login("Nzk1MjY4NjEzNjk5MTQxNjgy.X_G53g.dCpi6A9bXRlNe4otjyPH3SMFzfU");
 ta6.login("Nzk1MjY5MzM2MTk3NjI3OTE2.X_G6aQ.dcTh4DW-Jhbgoo9lk9Spk3hIwoQ");
 ta7.login("Nzk1MjcwMTM0NjMyODA4NDQ4.X_G7QA.Ke9ThMmu2iHoYyDSDoMN8-MQ0PI");
 ta8.login("Nzk1MjcxMDYxMTcxMDc3MTgx.X_G8Vg.uLtkeeMB5naORQ_it0XYHfkuIlw");
 ta9.login("Nzk1MjcyNDM0MDg5NTkwODA0.X_G9Pw.dZYB1V0XdJUOOxPegEDPo7lLiJQ");
ta10.login("Nzk1MjcyOTMxNDE2NDczNjUw.X_G9vQ.P76Hcjxp_xe787av1_QJXzvzwv0");
ta11.login("Nzk1MjczMzk5MjM1NzcyNDM2.X_G-QA.JUAdcStgiD-6MUcuz5Fku7KGrtY");
ta12.login("Nzk2MDI5MDE3NzU2Nzk0ODky.X_R-QA.vIUxQrrPA_fx3UVzlYtdVcKAwRg");
ta13.login("Nzk2MDI4MzIxMTgzMDM5NTQ5.X_R9YQ.LPbJalBKC0cziv40kr-XkbNUKsk");
ta14.login("Nzk2MDI3Mzg4MTU2NTc1ODE1.X_R81Q.BmEZYIJS6JVQpV9ObBnuvs9r1_M");
ta15.login("Nzk1Mjc4NzQxMjQ4NjcxNzk0.X_HDNQ.LoZYdqWXMHsaTFQMmaif4QlYWt8");
ta16.login("Nzk1Mjc3OTg2MjE5MTYzNjY5.X_HCog.WIourV3eEXs9D0rgkuosPLtZR-U");
ta17.login("Nzk1Mjc3NTE5MTA4MDQ2ODQ5.X_HCDg.k4ArHKv5emLbEyc2WeiVcAlzLw8");
ta18.login("Nzk1Mjc1NjY5MTcyOTEyMTQ5.X_HAYA.hyRUF2M-_OCCv6Q9dMRSaKVr2M0");
ta19.login("Nzk1Mjc1MTg5NDg2NzQ3NjU5.X_G_zA.cZo1YtIXLXj7vbh-MhF1VWMaINM");
ta20.login("Nzk1Mjc0NTI4NDg0ODg0NTEw.X_G_Sw.uEFfx71fSPXS_0eHksZG9qOVD5Q");
ta21.login("Nzk1MjczOTQyNDgxNzY0Mzgz.X_G-wA.1WX8gLFMRao0tRUVr5Yc7_SZA8I");
ta22.login("Nzk1MjczMzk5MjM1NzcyNDM2.X_G-QA.JUAdcStgiD-6MUcuz5Fku7KGrtY");
ta23.login("Nzk1MjcyOTMxNDE2NDczNjUw.X_G9vQ.P76Hcjxp_xe787av1_QJXzvzwv0");
ta24.login("Nzk2Njc1NjU5Mzg0MjI1ODcy.X_bYag.muIajw8zctmUd3Q3GxgmmfY-jKc");
ta25.login("Nzk2Njc3MDQ4NTQ2MzYxMzU0.X_baRQ.7j4MnM3aeJGDNQqRJnm8vajgJcM");
ta26.login("Nzk2Njc4NTg0Mjk0NTcyMDMz.X_bbYw.VwWG_hoVcia7E4QyeOiHedFs_Ls");
ta27.login("NzkxOTk3Nzg4Mzg0MjY0MjIy.X_bp-w.q8PMhAlNp_v3KEMWHFhqBgMSjl8");
ta28.login("Nzk3OTMzODk3MDU1ODYyODI1.X_tsPw.RUcSy2UeI19lhIl7Kgs8v9XqYTE");
ta29.login("Nzk3OTM0OTA3Njg1OTk0NTA3.X_ttBA.GqAd4dh_uv898BQBiQqpI78JBdg");
ta30.login("");
ta31.login("");
ta32.login("");
ta33.login("");
ta34.login("");
ta35.login("");
ta36.login("");
ta37.login("");
ta38.login("");
ta39.login("");
ta40.login("");
ta41.login("");
ta42.login("");
ta43.login("");
ta44.login("");
ta45.login("");
ta46.login("");
ta47.login("");
ta48.login("");
ta49.login("");
ta50.login("");
ta51.login("");
ta52.login("");
ta53.login("");
ta54.login("");
ta55.login("");
ta56.login("");
ta57.login("");
ta58.login("");
ta59.login("");
ta60.login("");
ta61.login("");

client.on("ta", async function() {
const voiceChannel = "767136446661525509"
client.channels.cache.get(voiceChannel).join()
.catch(err => {
throw err;
})
})

ta1.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 5 };
    var kanal = msg.member.voiceChannel;
    kanal.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=pR5-4z201YU", {
        filter: "audioonly"
      })
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});



ta2.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 100, volume: 100 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=pR5-4z201YU", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta3.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 2};
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=pR5-4z201YU", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta4.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta5.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta6.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta7.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 10 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg",   {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta8.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta9.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta10.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta11.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta12.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta13.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta14.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta15.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta16.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 1500 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta17.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta18.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta19.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta20.on("message", async msg => {
  if (msg.content.toLowerCase() === "gelin") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta21.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta22.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta23.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta24.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta25.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta26.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta27.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta28.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta29.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta30.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta31.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta32.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta33.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta34.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta35.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta36.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta37.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta38.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta39.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta40.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta41.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta42.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta43.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta44.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta45.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta46.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta47.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta48.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta49.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta50.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta51.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta52.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta53.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta54.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta55.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta56.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta57.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta58.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta59.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta60.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta61.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta62.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta63.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta64.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta65.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta66.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta67.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta68.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta69.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta70.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta71.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta72.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta73.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta74.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta75.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta76.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta77.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta78.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta79.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta80.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta81.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta82.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta83.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta84.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta85.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta86.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta87.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta88.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta89.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta90.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});

ta91.on("message", async msg => {
  if (msg.content.toLowerCase() === "t!bekliyorum") {
    const streamOptions = { seek: 5, volume: 0 };
    var voiceChannel = msg.member.voiceChannel;
    voiceChannel.join().then(connection => {
      const stream = ytdl("https://www.youtube.com/watch?v=-zYPp_MJ9Wg", {
        filter: "audioonly"
      });
      const dispatcher = connection.playStream(stream, streamOptions);
    });
  }
});


client.on('ready', () => {
    client.user.setStatus('available')
    client.user.setPresence({
        game: {
            name: 'Armisa',
            type: "STREAMING",
            url: "https://www.twitch.tv/armisa_"
        }
    });
});